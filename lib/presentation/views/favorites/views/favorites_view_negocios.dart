import 'package:flutter/material.dart';

class FavoritesViewNegocios extends StatelessWidget {
  const FavoritesViewNegocios({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('FavoritesNegocios'),
    );
  }
}
